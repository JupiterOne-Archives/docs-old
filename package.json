{
  "name": "@jupiterone/docs",
  "version": "1.3.0",
  "description": "JupiterOne documentation",
  "files": [
    "dashboards/**/*",
    "docs/**/*",
    "faqs/**/*",
    "guides/**/*",
    "queries/**/*",
    "src/**/*",
    "zendesk/links.json"
  ],
  "main": "src/index.js",
  "dependencies": {
    "js-yaml": "^3.13.1",
    "request": "^2.88.0",
    "request-promise-native": "^1.0.7",
    "showdown": "^1.9.0"
  },
  "scripts": {
    "buildDocs": "yarn ts-node src/util/buildDataModelDocs.ts",
    "publish:zendesk": "export $(grep -v '^#' .env | xargs); cd zendesk; node -r dotenv/config publish.js"
  },
  "devDependencies": {
    "@jupiterone/data-model": "^0.22.0",
    "@types/node": "^15.3.0",
    "dotenv": "^8.0.0",
    "ts-node": "^9.1.1",
    "typescript": "^4.2.4"
  }
}
